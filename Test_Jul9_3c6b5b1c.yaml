- name: Install Update on Windows Server 2008 R2
  hosts: windows_servers
  become: yes
  tasks:
    - name: Navigate to the directory where the update file was downloaded
      win_shell: |
        cd C:\Downloads
      register: change_directory

    - name: Install the update
      win_shell: |
        msiexec /i {{ update_file }}
      vars:
        update_file: "C:\\Downloads\\update_file.msi"